rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Only authenticated users can read/write startups
    match /startups/{startupId} {
      allow read, write: if request.auth != null;
    }
  }
}
